export default function HomePage() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: 32 }}>
      <h1>GitHub Analyzer API</h1>
      <p>Use POST /api/analyze with a GitHub repository URL.</p>
    </main>
  );
}
